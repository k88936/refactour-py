this is a demo project for education purposes to teach refactor and code smell.
it is a fake voice typer.
for test purpose, it doesn't function, only receive mocked event from stdin and output response to stdout.
those _impl.py(NEVER READ OR MODIFY), api.py should be seen as external (mocked) dependencies. 